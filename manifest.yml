---
declared-services:
  text-to-speech-service:
    label: text_to_speech
    plan: standard
applications:
- services:
  - MyTextToSpeech
  name: NewKnFirstApp
  path: .
  memory: 128M
  env:
    NODE_ENV: production
    SECURE_EXPRESS: 1
